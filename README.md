# Belay, a flexible MicroPython interface for experimental hardware control

## Quick start
- Clone the repository
- Install [Pixi](https://pixi.sh)
- Run `pixi run preview`; this will:
    - Install the dependencies
    - Bring up the HTML preview
- To see the PDF, click `Typst (lepub)` in the top of the article